# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class SupplierActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Supplier]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Supplier.prisma().query_raw(
            'SELECT * FROM Supplier WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Supplier
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Supplier.prisma().query_first(
            'SELECT * FROM Supplier WHERE url = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SupplierCreateInput,
        include: Optional[types.SupplierInclude] = None
    ) -> _PrismaModelT:
        """Create a new Supplier record.

        Parameters
        ----------
        data
            Supplier record data
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The created Supplier record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Supplier record from just the required fields
        supplier = await Supplier.prisma().create(
            data={
                # data to create a Supplier record
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SupplierCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Supplier records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Supplier record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Supplier.prisma().create_many(
            data=[
                {
                    # data to create a Supplier record
                },
                {
                    # data to create a Supplier record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SupplierWhereUniqueInput,
        include: Optional[types.SupplierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Supplier record.

        Parameters
        ----------
        where
            Supplier filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The deleted Supplier record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        supplier = await Supplier.prisma().delete(
            where={
                'id': 'ggciceaie',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SupplierWhereUniqueInput,
        include: Optional[types.SupplierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Supplier record.

        Parameters
        ----------
        where
            Supplier filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The found Supplier record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        supplier = await Supplier.prisma().find_unique(
            where={
                'id': 'bbehjachib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SupplierWhereUniqueInput,
        include: Optional[types.SupplierInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Supplier record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Supplier filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The found Supplier record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        supplier = await Supplier.prisma().find_unique_or_raise(
            where={
                'id': 'cadfabfehe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
        include: Optional[types.SupplierInclude] = None,
        order: Optional[Union[types.SupplierOrderByInput, List[types.SupplierOrderByInput]]] = None,
        distinct: Optional[List[types.SupplierScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Supplier records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Supplier records returned
        skip
            Ignore the first N results
        where
            Supplier filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Supplier model
        order
            Order the returned Supplier records by any field
        distinct
            Filter Supplier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Supplier]
            The list of all Supplier records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Supplier records
        suppliers = await Supplier.prisma().find_many(take=10)

        # find the first 5 Supplier records ordered by the name field
        suppliers = await Supplier.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
        include: Optional[types.SupplierInclude] = None,
        order: Optional[Union[types.SupplierOrderByInput, List[types.SupplierOrderByInput]]] = None,
        distinct: Optional[List[types.SupplierScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Supplier record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Supplier filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Supplier model
        order
            Order the returned Supplier records by any field
        distinct
            Filter Supplier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Supplier
            The first Supplier record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Supplier record ordered by the riskScore field
        supplier = await Supplier.prisma().find_first(
            skip=1,
            order={
                'riskScore': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
        include: Optional[types.SupplierInclude] = None,
        order: Optional[Union[types.SupplierOrderByInput, List[types.SupplierOrderByInput]]] = None,
        distinct: Optional[List[types.SupplierScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Supplier record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Supplier filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Supplier model
        order
            Order the returned Supplier records by any field
        distinct
            Filter Supplier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Supplier
            The first Supplier record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Supplier record ordered by the summary field
        supplier = await Supplier.prisma().find_first_or_raise(
            skip=1,
            order={
                'summary': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SupplierUpdateInput,
        where: types.SupplierWhereUniqueInput,
        include: Optional[types.SupplierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Supplier record.

        Parameters
        ----------
        data
            Supplier record data specifying what to update
        where
            Supplier filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The updated Supplier record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        supplier = await Supplier.prisma().update(
            where={
                'id': 'dgiiaaijj',
            },
            data={
                # data to update the Supplier record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SupplierWhereUniqueInput,
        data: types.SupplierUpsertInput,
        include: Optional[types.SupplierInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Supplier filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Supplier model

        Returns
        -------
        prisma.models.Supplier
            The created or updated Supplier record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        supplier = await Supplier.prisma().upsert(
            where={
                'id': 'bfaiacjjfc',
            },
            data={
                'create': {
                    'id': 'bfaiacjjfc',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SupplierUpdateManyMutationInput,
        where: types.SupplierWhereInput,
    ) -> int:
        """Update multiple Supplier records

        Parameters
        ----------
        data
            Supplier data to update the selected Supplier records to
        where
            Filter to select the Supplier records to update

        Returns
        -------
        int
            The total number of Supplier records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Supplier records
        total = await Supplier.prisma().update_many(
            data={
                'flags': ['eigcfgbif']
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Supplier records present in the database

        Parameters
        ----------
        select
            Select the Supplier fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Supplier filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SupplierCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Supplier.prisma().count()

        # results: prisma.types.SupplierCountAggregateOutput
        results = await Supplier.prisma().count(
            select={
                '_all': True,
                'analysisData': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SupplierCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
    ) -> types.SupplierCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SupplierCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SupplierWhereInput] = None,
        cursor: Optional[types.SupplierWhereUniqueInput] = None,
    ) -> Union[int, types.SupplierCountAggregateOutput]:
        """Count the number of Supplier records present in the database

        Parameters
        ----------
        select
            Select the Supplier fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Supplier filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SupplierCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Supplier.prisma().count()

        # results: prisma.types.SupplierCountAggregateOutput
        results = await Supplier.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SupplierCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SupplierWhereInput] = None
    ) -> int:
        """Delete multiple Supplier records.

        Parameters
        ----------
        where
            Optional Supplier filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Supplier records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Supplier records
        total = await Supplier.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SupplierScalarFieldKeys'],
        *,
        where: Optional['types.SupplierWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SupplierAvgAggregateInput'] = None,
        sum: Optional['types.SupplierSumAggregateInput'] = None,
        min: Optional['types.SupplierMinAggregateInput'] = None,
        max: Optional['types.SupplierMaxAggregateInput'] = None,
        having: Optional['types.SupplierScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SupplierCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SupplierScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SupplierScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SupplierGroupByOutput']:
        """Group Supplier records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Supplier fields to group records by
        where
            Supplier filter to select records
        take
            Limit the maximum number of Supplier records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SupplierGroupByOutput]
            A list of dictionaries representing the Supplier record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Supplier records by updatedAt values
        # and count how many records are in each group
        results = await Supplier.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ChatLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ChatLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ChatLog.prisma().query_raw(
            'SELECT * FROM ChatLog WHERE id = $1',
            'bagcfbhiig',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ChatLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ChatLog.prisma().query_first(
            'SELECT * FROM ChatLog WHERE url = $1',
            'cghideieh',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ChatLogCreateInput,
        include: Optional[types.ChatLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new ChatLog record.

        Parameters
        ----------
        data
            ChatLog record data
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The created ChatLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ChatLog record from just the required fields
        chatlog = await ChatLog.prisma().create(
            data={
                # data to create a ChatLog record
                'question': 'biabhbdai',
                'answer': 'idghgaicb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ChatLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ChatLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ChatLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ChatLog.prisma().create_many(
            data=[
                {
                    # data to create a ChatLog record
                    'question': 'fjfddhigg',
                    'answer': 'hjaecfifb',
                },
                {
                    # data to create a ChatLog record
                    'question': 'cbbbjbfcii',
                    'answer': 'bbejhfidcb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ChatLogWhereUniqueInput,
        include: Optional[types.ChatLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ChatLog record.

        Parameters
        ----------
        where
            ChatLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The deleted ChatLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatlog = await ChatLog.prisma().delete(
            where={
                'id': 'bgeecijdgg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ChatLogWhereUniqueInput,
        include: Optional[types.ChatLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ChatLog record.

        Parameters
        ----------
        where
            ChatLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The found ChatLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatlog = await ChatLog.prisma().find_unique(
            where={
                'id': 'bdiicjafbj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ChatLogWhereUniqueInput,
        include: Optional[types.ChatLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ChatLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ChatLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The found ChatLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatlog = await ChatLog.prisma().find_unique_or_raise(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
        include: Optional[types.ChatLogInclude] = None,
        order: Optional[Union[types.ChatLogOrderByInput, List[types.ChatLogOrderByInput]]] = None,
        distinct: Optional[List[types.ChatLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ChatLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ChatLog records returned
        skip
            Ignore the first N results
        where
            ChatLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatLog model
        order
            Order the returned ChatLog records by any field
        distinct
            Filter ChatLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ChatLog]
            The list of all ChatLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ChatLog records
        chatlogs = await ChatLog.prisma().find_many(take=10)

        # find the first 5 ChatLog records ordered by the question field
        chatlogs = await ChatLog.prisma().find_many(
            take=5,
            order={
                'question': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
        include: Optional[types.ChatLogInclude] = None,
        order: Optional[Union[types.ChatLogOrderByInput, List[types.ChatLogOrderByInput]]] = None,
        distinct: Optional[List[types.ChatLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ChatLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ChatLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatLog model
        order
            Order the returned ChatLog records by any field
        distinct
            Filter ChatLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ChatLog
            The first ChatLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ChatLog record ordered by the answer field
        chatlog = await ChatLog.prisma().find_first(
            skip=1,
            order={
                'answer': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
        include: Optional[types.ChatLogInclude] = None,
        order: Optional[Union[types.ChatLogOrderByInput, List[types.ChatLogOrderByInput]]] = None,
        distinct: Optional[List[types.ChatLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ChatLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ChatLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatLog model
        order
            Order the returned ChatLog records by any field
        distinct
            Filter ChatLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ChatLog
            The first ChatLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ChatLog record ordered by the sources field
        chatlog = await ChatLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'sources': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ChatLogUpdateInput,
        where: types.ChatLogWhereUniqueInput,
        include: Optional[types.ChatLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ChatLog record.

        Parameters
        ----------
        data
            ChatLog record data specifying what to update
        where
            ChatLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The updated ChatLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        chatlog = await ChatLog.prisma().update(
            where={
                'id': 'bghffegacj',
            },
            data={
                # data to update the ChatLog record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ChatLogWhereUniqueInput,
        data: types.ChatLogUpsertInput,
        include: Optional[types.ChatLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ChatLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ChatLog model

        Returns
        -------
        prisma.models.ChatLog
            The created or updated ChatLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatlog = await ChatLog.prisma().upsert(
            where={
                'id': 'bhghchehcc',
            },
            data={
                'create': {
                    'id': 'bhghchehcc',
                    'question': 'cbbbjbfcii',
                    'answer': 'bbejhfidcb',
                },
                'update': {
                    'question': 'cbbbjbfcii',
                    'answer': 'bbejhfidcb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ChatLogUpdateManyMutationInput,
        where: types.ChatLogWhereInput,
    ) -> int:
        """Update multiple ChatLog records

        Parameters
        ----------
        data
            ChatLog data to update the selected ChatLog records to
        where
            Filter to select the ChatLog records to update

        Returns
        -------
        int
            The total number of ChatLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ChatLog records
        total = await ChatLog.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ChatLog records present in the database

        Parameters
        ----------
        select
            Select the ChatLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ChatLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ChatLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ChatLog.prisma().count()

        # results: prisma.types.ChatLogCountAggregateOutput
        results = await ChatLog.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ChatLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
    ) -> types.ChatLogCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ChatLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatLogWhereInput] = None,
        cursor: Optional[types.ChatLogWhereUniqueInput] = None,
    ) -> Union[int, types.ChatLogCountAggregateOutput]:
        """Count the number of ChatLog records present in the database

        Parameters
        ----------
        select
            Select the ChatLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ChatLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ChatLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ChatLog.prisma().count()

        # results: prisma.types.ChatLogCountAggregateOutput
        results = await ChatLog.prisma().count(
            select={
                '_all': True,
                'url': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ChatLogCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ChatLogWhereInput] = None
    ) -> int:
        """Delete multiple ChatLog records.

        Parameters
        ----------
        where
            Optional ChatLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ChatLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ChatLog records
        total = await ChatLog.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ChatLogScalarFieldKeys'],
        *,
        where: Optional['types.ChatLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ChatLogAvgAggregateInput'] = None,
        sum: Optional['types.ChatLogSumAggregateInput'] = None,
        min: Optional['types.ChatLogMinAggregateInput'] = None,
        max: Optional['types.ChatLogMaxAggregateInput'] = None,
        having: Optional['types.ChatLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ChatLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ChatLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ChatLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ChatLogGroupByOutput']:
        """Group ChatLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ChatLog fields to group records by
        where
            ChatLog filter to select records
        take
            Limit the maximum number of ChatLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ChatLogGroupByOutput]
            A list of dictionaries representing the ChatLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ChatLog records by question values
        # and count how many records are in each group
        results = await ChatLog.prisma().group_by(
            ['question'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models