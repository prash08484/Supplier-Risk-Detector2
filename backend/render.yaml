services:
  - type: web
    name: supplier-risk-detector
    env: python
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    environment:
      pythonVersion: 3.12.8
